# UGV 控制通路验证工具 — 开发进度与测试记录

## 当前状态：`v0.1 — 初始实现完成，待硬件测试`

---

## 里程碑

| 版本 | 状态 | 日期 | 说明 |
|------|------|------|------|
| v0.1 | ✅ 完成 | 2026-03-10 | 初始实现；干跑（dry_run）逻辑验证 |
| v0.2 | ⬜ 待做 | — | Jetson 真实硬件首次联调 |
| v0.3 | ⬜ 待做 | — | 根据联调结果调整参数（控制频率、速度档等） |

---

## v0.1 实现清单

- [x] `ctrl_test.py` 主脚本（单文件，~270 行）
- [x] 控制主循环（`control_hz=10Hz`）
- [x] 复用 `ugv_data_collector/robots/` 和 `teleop/` 模块
- [x] 图片采样（`image_interval=5s`，JPEG 写入）
- [x] CSV 日志（`log_interval=1s`，含 vx/wz 指令与反馈、里程计、电压）
- [x] 终端 `\r` 原地刷新状态行
- [x] 退出时保存最终帧
- [x] `--dry_run` 支持（虚拟底盘，无需真实串口/摄像头）
- [x] `README.md` 使用文档
- [x] `DESIGN.md` 需求与设计文档
- [x] `PROGRESS.md` 本文件

---

## 待测试项

### 优先级 P0（必须通过，才能用于正式采集前验证）

- [ ] **T1**：`--dry_run` 模式正常启动，终端状态行刷新
- [ ] **T2**：`--dry_run` 模式下 W/S/A/D 按键有速度数值变化（`vx_cmd`/`wz_cmd`）
- [ ] **T3**：`--dry_run` 下 Esc 退出，`output/` 目录生成，图片和 CSV 不为空
- [ ] **T4**：真实硬件 — 串口连接成功，底盘有运动响应
- [ ] **T5**：真实硬件 — Space 急停后底盘立即停止
- [ ] **T6**：真实硬件 — `ctrl_log.csv` 中 `vx_actual`/`wz_actual` 与 `vx_cmd`/`wz_cmd` 趋势一致
- [ ] **T7**：真实硬件 — 图片内容清晰，时间戳与运动状态对应

### 优先级 P1（完整验证）

- [ ] **T8**：Q/E 速度档切换正确，CSV 中 `speed_scale` 列变化
- [ ] **T9**：电池电压 `voltage_V` 读取合理（Jetson UGV 标称 14.8V 左右）
- [ ] **T10**：长时间运行（>5 分钟）无内存泄漏，CSV 行数持续增长
- [ ] **T11**：Ctrl+C 中断时最终帧被保存

---

## 已知问题 / 待解决

| # | 描述 | 优先级 | 状态 |
|---|------|--------|------|
| I1 | `on_episode_end=None` 传给 `KeyboardUGVTeleop`，需确认 Enter 键不触发异常 | P0 | ✅ 已确认安全：keyboard_teleop.py 中有 `if self.on_episode_end:` 守护 |
| I2 | `rover._latest_feedback` 为受保护成员，直接访问耦合实现细节 | P2 | 暂接受，后续可在 `UGVRover` 增加 `get_raw_feedback()` 公开接口 |

---

## 测试记录

### T1～T3 dry_run 测试

> 日期：2026-03-10  
> 测试人：Copilot（静态验证）  
> 环境：Jetson / Python 语法检查 + --help 输出

| 项目 | 结果 | 备注 |
|------|------|------|
| 启动无报错 | ✅ | `py_compile` 通过 |
| --help 正常 | ✅ | 所有参数显示正确 |
| WASD 有数值变化 | ⬜ | 待实机运行确认 |
| Esc 退出流程 | ⬜ | 待实机运行确认 |
| output 目录生成 | ⬜ | 待实机运行确认 |

### T4～T7 真实硬件联调

> 日期：  
> 测试人：  
> 环境：Jetson Orin / 串口 `/dev/ttyTHS1` / 摄像头 `/dev/video0`

| 项目 | 结果 | 备注 |
|------|------|------|
| 串口连接 | ⬜ | |
| 底盘运动响应 | ⬜ | |
| 急停效果 | ⬜ | |
| 速度指令与反馈对比 | ⬜ | |
| 图片清晰度 | ⬜ | |
